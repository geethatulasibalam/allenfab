<% url = spree.product_path(product, taxon_id: taxon.try(:id)) %>
<div id="product_<%= product.id %>" class="product" data-hook="products_list_item">
  <div class="card product-card text-center">
    <% cache(taxon.present? ? [I18n.locale, current_currency, taxon, product] : cache_key_for_product(product)) do %>
      <div class="card-body product-body">
        <%= link_to url do %>
          <%= small_image(product,class: "product-image") %> 
          <p class="product-name">
          <%= content_tag(:span, truncate(product.name, length: 50), title: product.name)%>
          </p>
        <% end %>
      </div>
      <div class="card-footer text-center">
        <span>
          <span class="price selling lead">
            <%= display_price(product) %>
          </span>
        </span>
      </div>
    <% end %>
  </div>
</div> 
  
